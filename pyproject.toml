[project]
name = "mypkg"
version = "0.1.0"
description = "Lecturepackage"
authors = [
    {name = "KUWANO Hiroaki"}
]
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
dependencies = []